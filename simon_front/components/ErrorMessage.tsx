const ErrorMessage = () => {
    return (
        <div className="w-full h-full flex items-center justify-center">
            <p className="w-12 h-12 text-red-500">Se ha producido un error</p>
        </div>
    );
};

export default ErrorMessage;
